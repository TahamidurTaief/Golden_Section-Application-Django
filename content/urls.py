from django.urls import path

app_name = 'content'

urlpatterns = [
    # Content page URLs will go here
    # path('about/', views.about, name='about'),
    # path('contact/', views.contact, name='contact'),
    # path('<slug:slug>/', views.page_detail, name='page'),
]
